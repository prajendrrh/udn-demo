# Deployments on the shared CUDN (100.1.0.0/24) in each namespace.
# UBI minimal; connectivity tested via TCP (no NET_RAW).
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: team-platform-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-cudn
  template:
    metadata:
      labels:
        app: app-cudn
    spec:
      containers:
        - name: app
          image: quay.io/rh_ee_prajendr/ubi-minimal-nettools:latest
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: team-platform-staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-cudn
  template:
    metadata:
      labels:
        app: app-cudn
    spec:
      containers:
        - name: app
          image: quay.io/rh_ee_prajendr/ubi-minimal-nettools:latest
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: team-platform-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-cudn
  template:
    metadata:
      labels:
        app: app-cudn
    spec:
      containers:
        - name: app
          image: quay.io/rh_ee_prajendr/ubi-minimal-nettools:latest
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
