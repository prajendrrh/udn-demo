# Use case 4: Pods on UDN + NetworkPolicy (VM optional)
# NetworkPolicy is optional; remove from resources if you only want the UDN.
resources:
  - namespace.yaml
  - udn.yaml
  - network-policy-deny-other-namespaces.yaml
  - deployment.yaml
