# Namespace for UDN + NetworkPolicy demo (pods; optionally VMs if OpenShift Virtualization is installed).
apiVersion: v1
kind: Namespace
metadata:
  name: udn-vm-demo
  labels:
    k8s.ovn.org/primary-user-defined-network: ""
