# Network policies with UDN: both namespaces on the same UDN (one CUDN) so they can route;
# NetworkPolicy allows only np-udn-b → server in np-udn-a.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - cudn-shared.yaml
  - deployment-server.yaml
  - deployment-client.yaml
  - network-policy-allow-from-b.yaml
