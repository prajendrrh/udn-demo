# Multihomed pods: primary = UDN (namespace), secondary = UDN (annotation).
# Each pod has two interfaces: primary 100.5.0.x (udn-primary) and secondary 100.6.0.x (udn-secondary).
# Image and TCP-based tests aligned with use cases 1–4 (no NET_RAW).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-multihomed
  namespace: multihoming-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: multihomed
  template:
    metadata:
      labels:
        app: multihomed
      annotations:
        k8s.v1.cni.cncf.io/networks: multihoming-demo/udn-secondary
    spec:
      containers:
        - name: app
          image: quay.io/rh_ee_prajendr/ubi-minimal-nettools:latest
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
