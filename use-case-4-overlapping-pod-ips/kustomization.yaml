# Overlapping pod IPs
# Two namespaces, two UDNs, same subnet (100.4.0.0/24). Pods in each namespace get IPs from the same range; no conflict because each UDN is a separate logical network.
resources:
  - namespaces.yaml
  - udn-overlapping-a.yaml
  - udn-overlapping-b.yaml
  - deployment.yaml
