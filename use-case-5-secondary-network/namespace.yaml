# Namespace for multihoming: primary UDN + secondary NAD.
# Label makes this namespace use a UserDefinedNetwork as primary; pods also attach a secondary via annotation.
apiVersion: v1
kind: Namespace
metadata:
  name: multihoming-demo
  labels:
    k8s.ovn.org/primary-user-defined-network: ""
