# Use case 5: Multihoming (primary UDN + secondary NAD)
# Pods have two interfaces: primary from UserDefinedNetwork, secondary from NetworkAttachmentDefinition.
resources:
  - namespace.yaml
  - udn-primary.yaml
  - network-attachment-definition.yaml
  - deployment-with-secondary.yaml
  - deployment-test-helper.yaml
  # MultiNetworkPolicy (optional): enable useMultiNetworkPolicy on cluster, then uncomment:
  # - multi-network-policy-example.yaml
