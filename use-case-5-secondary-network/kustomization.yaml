# Use case 5: Multihoming (primary UDN + secondary UDN)
# Pods have two interfaces: primary from UserDefinedNetwork (udn-primary), secondary from UserDefinedNetwork (udn-secondary) via annotation.
resources:
  - namespace.yaml
  - udn-primary.yaml
  - udn-secondary.yaml
  - deployment-with-secondary.yaml
  # MultiNetworkPolicy (optional): enable useMultiNetworkPolicy on cluster, then uncomment (targets secondary UDN):
  # - multi-network-policy-example.yaml
